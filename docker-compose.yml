version: '3.8'
name: hangbuddies
services:
    mongodb:
        image: mongo
        container_name: mongodb
        ports:
            - '27017:27017'
        volumes:
            - mongodb_data:/data/db
        environment:
            MONGO_INITDB_DATABASE: hangbuddies
volumes:
    mongodb_data:
